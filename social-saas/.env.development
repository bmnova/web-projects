# Loaded automatically by Next.js when you run `next dev` (not used in production build).
# Sample MP4s instead of Veo — override in .env.development.local (gitignored) if you need real video locally.
MOCK_VIDEO_GENERATION=true
# Unlimited AI generations while testing (server-only; never set in production).
DISABLE_AI_QUOTA=true
